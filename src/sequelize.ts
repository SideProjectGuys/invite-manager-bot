// ------------------------------------
// Presences
// ------------------------------------
export enum PresenceStatus {
	online = 'online',
	offline = 'offline',
	idle = 'idle',
	dnd = 'dnd'
}

export enum GameType {
	playing = 0,
	streaming = 1,
	listening = 2
}
