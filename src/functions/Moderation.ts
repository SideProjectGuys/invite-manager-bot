import { Message } from '@yamdbf/core';

export function AutoModeration(message: Message): boolean {
	// Check for links
	// Check for blacklisted words
	// Check for mention spam
	// Check for all caps
	// etc
	return true;
}
